const app=document.getElementById('app');
const KEY='merveil_dev_projects_v5';
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function passport(){try{return JSON.parse(localStorage.getItem('merveil_digital_passport_v1')||'null')}catch{return null}}
function projects(){try{const x=JSON.parse(localStorage.getItem(KEY)||'[]');return Array.isArray(x)?x:[]}catch{return []}}
function status(p){if(p?.published||p?.interfacePublished)return 'Published';if(p?.status)return String(p.status);if(p?.ready||p?.buildStatus==='ready')return 'Ready';if(p?.building||p?.buildStatus==='building')return 'Building';return 'Draft'}
function projectId(p){return p?.developer_project_id||p?.developerProjectId||p?.id||''}
function render(){const p=passport()||{};const name=p.full_name||p.name||p.company||'Verified Citizen';const ps=projects();const latest=ps[0];const stage=latest?`${esc(latest.name||latest.title||'Your project')} · ${esc(status(latest))}`:'Your next creation is waiting';app.innerHTML=`<div class="shell"><header class="top"><div class="brand"><div class="mark">✦</div><div class="brand-name">Merveil</div></div><div class="passport">${esc(name)} · Passport</div></header><section class="hero"><div class="eyebrow">A living space for ideas</div><h1>Make it <span>real.</span></h1><p>Merveil helps you discover what is possible, then turn what you imagine into something you can build, shape and share.</p><div class="actions"><button class="orb discover" id="discover"><div class="orb-icon">🤔</div><strong>Discover</strong><small>I don't know what to build.</small></button><button class="orb create" id="create"><div class="orb-icon">✦</div><strong>Create</strong><small>I know what I want to make.</small></button></div></section><section class="workspace"><div class="workspace-head"><div><div class="workspace-title">${stage}</div><div class="workspace-sub">Your work stays in the same Merveil space.</div></div><div class="workspace-tools"><button class="tool" id="debug">⌁ Debug</button><button class="tool" id="boost">↗ Boost</button></div></div><div class="empty-stage"><div><h2>${latest?'Continue where you left off.':'What will you make today?'}</h2><p>${latest?'Open your latest creation and keep shaping it with Merveil.':'Start with an idea, a problem, a story, a business or simply a feeling.'}</p></div></div></section><nav class="bottom-nav"><a class="nav-pill" href="/developer/beginner">Open workspace</a><a class="nav-pill" href="/interface">Explore Interface</a><a class="nav-pill" href="/developer/pro">Build without AI</a></nav></div>`;document.getElementById('discover').onclick=()=>location.href='/developer/beginner?mode=discover';document.getElementById('create').onclick=()=>location.href='/developer/beginner';document.getElementById('debug').onclick=()=>location.href='/developer/debug';document.getElementById('boost').onclick=()=>location.href='/developer/boost'}
render();