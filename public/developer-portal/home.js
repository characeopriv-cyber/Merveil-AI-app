const app=document.getElementById('app');
const KEY='merveil_dev_projects_v5';
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function passport(){try{return JSON.parse(localStorage.getItem('merveil_digital_passport_v1')||'null')}catch{return null}}
function projects(){try{const x=JSON.parse(localStorage.getItem(KEY)||'[]');return Array.isArray(x)?x:[]}catch{return []}}
function status(p){if(p?.published||p?.interfacePublished)return 'Published';if(p?.status)return String(p.status);if(p?.ready||p?.buildStatus==='ready')return 'Ready';if(p?.building||p?.buildStatus==='building')return 'Building';return 'Draft'}
function projectId(p){return p?.developer_project_id||p?.developerProjectId||p?.id||''}
function render(){const p=passport()||{};const name=p.full_name||p.name||p.company||'Verified Citizen';const photo=p.photo_url||p.avatar_url||p.photo||'';const ps=projects();const cards=ps.slice(0,8).map(x=>`<article class="project"><div><b>${esc(x.name||x.title||'Untitled project')}</b><small>${esc(status(x))} · ${Object.keys(x.files||{}).length} files</small></div><div class="project-actions"><a href="/developer/pro">Open</a><button data-action="freeze" data-id="${esc(projectId(x))}">Freeze</button></div></article>`).join('')||'<div class="empty">No local projects yet. Start in Beginner or Pro Studio.</div>';app.innerHTML=`<div class="shell"><header class="top"><div class="identity"><img class="avatar" src="${esc(photo)}" onerror="this.style.visibility='hidden'" alt=""><div><div class="name">${esc(name)}</div><div class="verified">Digital Passport · Verified identity</div></div></div><div class="status"><i class="dot"></i> Online</div></header><section class="hero"><div class="eyebrow">Merveil Developer</div><h1>Build something real.</h1><p>One identity. One project space. Two ways to build — without AI when you want full control, or with Merveil beside you.</p><div class="rooms"><a class="room beginner" href="/developer/beginner"><b>Beginner</b><span>Describe your idea. Merveil turns it into a real, editable project.</span></a><a class="room" href="/developer/pro"><b>Pro Studio</b><span>Code normally while Merveil builds, explains and tests beside you.</span></a><a class="room" href="/developer/debug"><b>Debug</b><span>Diagnose and fix projects built here or systems outside Merveil.</span></a><a class="room" href="/developer/boost"><b>Boost</b><span>Grow a finished project with real audiences, channels and campaigns.</span></a></div></section><div class="projectbar"><h2>My Projects</h2><a class="link" href="/developer/beginner">Create →</a></div><section class="projects">${cards}</section><div class="profile"><button class="pill" id="profile">Profile</button><button class="pill" id="wallet">Wallet</button><button class="pill" id="settings">Settings</button><button class="pill" id="presence">Online / Offline</button><button class="pill" id="interface">Interface publishing</button></div><div class="note">Shared project identity: Build → Test → Ready → Interface → Boost. Interface publication is controlled by the project's verified state; this Home does not invent publication results.</div></div>`;document.querySelectorAll('[data-action="freeze"]').forEach(b=>b.onclick=()=>{const id=b.dataset.id;const all=projects();const q=all.find(x=>projectId(x)===id);if(q){q.status=q.status==='frozen'?'draft':'frozen';localStorage.setItem(KEY,JSON.stringify(all));render()}});document.getElementById('interface').onclick=()=>location.href='/interface';document.getElementById('profile').onclick=()=>alert(`${name}\nDigital Passport verified`);document.getElementById('wallet').onclick=()=>alert('Wallet is shared with the Merveil ecosystem.');document.getElementById('settings').onclick=()=>alert('Developer settings are shared with the Merveil identity.');document.getElementById('presence').onclick=()=>alert('Presence follows the same Merveil citizen session.')}
render();