import React, { useEffect, useMemo, useRef, useState } from "react";
import { createClient } from "@supabase/supabase-js";
import MerveilSecurityStatus from "./security/MerveilSecurityStatus.jsx";
import { localLinkGate, serverSafetyCheck } from "./security/merveilSafetyClient.js";

const SUPABASE_URL="https://dixfybqlepticyudikuz.supabase.co";
const SUPABASE_KEY="sb_publishable_zOtxwZ1q_OCpiTunktzypw_14pQnQOh";
const supabase=createClient(SUPABASE_URL,SUPABASE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});
const PASSPORTS={
 citizen:{label:"Citizen",sub:"Basic Passport",color:"#2F8F83",soft:"#E4F2EF"},
 professional:{label:"Professional",sub:"Professional Passport",color:"#3D72D8",soft:"#E6EEFB"},
 investor:{label:"Investor",sub:"Investor Passport",color:"#9A72C9",soft:"#EEE8F6"},
 company:{label:"Company",sub:"Company Passport",color:"#B77722",soft:"#F6EBDD"},
 services:{label:"Professional",sub:"Professional Passport",color:"#3D72D8",soft:"#E6EEFB"}
};
const styles=`.mcon{min-height:100dvh;background:#f7f5f1;color:#18202a;font-family:Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;padding:18px}.mcon *{box-sizing:border-box}.mcon-shell{max-width:1180px;margin:auto}.mcon-head{display:flex;justify-content:space-between;align-items:flex-end;gap:16px;margin-bottom:14px}.mcon-kicker{font-size:10px;letter-spacing:.16em;color:#738091;text-transform:uppercase}.mcon-title{font-size:25px;font-weight:850;letter-spacing:-.045em;margin-top:3px}.mcon-sub{font-size:12px;color:#687486;margin-top:5px}.mcon-card{background:rgba(255,255,255,.94);border:1px solid #e3e0d9;border-radius:20px;padding:16px;box-shadow:0 14px 38px rgba(38,42,47,.07)}.mcon-tabs{display:flex;gap:7px;margin-bottom:14px;overflow-x:auto}.mcon-tab{border:1px solid #ddd9d1;background:#fff;color:#667180;padding:9px 14px;border-radius:12px;white-space:nowrap;font-size:12px;cursor:pointer}.mcon-tab.active{background:#18202a;color:#fff;border-color:#18202a}.mcon-search{width:100%;background:#fff;border:1px solid #dedbd4;color:#18202a;border-radius:12px;padding:11px 13px;outline:none;margin-bottom:12px}.mcon-layout{display:grid;grid-template-columns:1.35fr .65fr;gap:14px}.mcon-group{margin-bottom:18px}.mcon-group-head{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:4px 2px 9px}.mcon-group-title{display:flex;align-items:center;gap:8px;font-size:13px;font-weight:850}.mcon-dot{width:9px;height:9px;border-radius:50%;flex:none}.mcon-count{font-size:11px;font-weight:800;padding:4px 8px;border-radius:999px}.mcon-person{display:flex;align-items:center;gap:11px;padding:12px 3px;border-top:1px solid #eeeae3;min-width:0}.mcon-avatar{width:43px;height:43px;border-radius:14px;display:grid;place-items:center;background:#eef0f2;border:1px solid #e0e2e4;font-weight:800;font-size:12px;flex:none}.mcon-main{min-width:0;flex:1}.mcon-name{font-size:13px;font-weight:800;line-height:1.25}.mcon-role{font-size:11px;color:#697585;margin-top:3px}.mcon-context{font-size:10px;color:#9199a4;margin-top:3px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.mcon-right{text-align:right}.mcon-pres{font-size:10px;font-weight:800}.mcon-pres.online{color:#238a68}.mcon-pres.away{color:#b77722}.mcon-pres.offline{color:#8d96a2}.mcon-actions{display:flex;gap:6px;margin-top:6px}.mcon-btn{border:1px solid #dedbd4;background:#fff;color:#34404c;border-radius:9px;padding:6px 8px;font-size:10px;cursor:pointer}.mcon-btn.primary{background:#18202a;border-color:#18202a;color:#fff}.mcon-message-row{display:flex;align-items:center;gap:11px;padding:13px 3px;border-top:1px solid #eeeae3;min-width:0;cursor:pointer}.mcon-message-main{min-width:0;flex:1}.mcon-message-name{font-size:13px;font-weight:850}.mcon-message-preview{font-size:11px;color:#707b89;margin-top:4px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.mcon-message-meta{font-size:10px;color:#8a94a1;text-align:right;white-space:nowrap}.mcon-unread{font-weight:850}.mcon-intel{position:sticky;top:12px}.mcon-intel p{font-size:11px;line-height:1.55;color:#6d7785;margin:14px 0}.mcon-suggestion{padding:11px;border-radius:13px;background:#f7f5f1;border:1px solid #e5e1d9;margin-top:8px}.mcon-suggestion strong{font-size:11px}.mcon-suggestion span{display:block;font-size:10px;color:#7b8592;line-height:1.45;margin-top:4px}.mcon-empty{text-align:center;color:#8b94a0;font-size:12px;padding:45px 20px}.mcon-more{width:100%;margin-top:10px;padding:10px;border:1px solid #ddd9d1;border-radius:10px;background:#fff;color:#4d5967;font-size:11px;cursor:pointer}.mcon-notice{position:sticky;top:8px;z-index:5;margin-bottom:10px;padding:10px 12px;border-radius:12px;background:#fff;border:1px solid #d8e9e3;box-shadow:0 8px 22px rgba(38,42,47,.08);font-size:11px;color:#34404c}.mcon-footer{margin-top:12px;font-size:10px;color:#9299a3;text-align:center}@media(max-width:800px){.mcon{padding:12px}.mcon-layout{grid-template-columns:1fr}.mcon-intel{position:static}}@media(max-width:520px){.mcon-title{font-size:21px}.mcon-card{border-radius:17px;padding:13px}.mcon-actions{display:none}}`;
function normalizeTier(v){const t=String(v||"citizen").toLowerCase();return PASSPORTS[t]?t:"citizen"}
function initials(n){return String(n||"M").split(" ").map(x=>x[0]).join("").slice(0,2).toUpperCase()}
function formatTime(v){if(!v)return"";const d=new Date(v);if(Number.isNaN(d.getTime()))return"";const diff=Date.now()-d.getTime();return diff<86400000?d.toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"}):d.toLocaleDateString([],{month:"short",day:"numeric"})}

export default function MerveilConnect({citizens=null,onOpenProfile=()=>{},onMessage=()=>{},onCall=()=>{},e2eeVerified=false}){
 const [tab,setTab]=useState("Citizens"),[query,setQuery]=useState(""),[list,setList]=useState(Array.isArray(citizens)?citizens:null),[messages,setMessages]=useState([]),[offset,setOffset]=useState(0),[hasMore,setHasMore]=useState(false),[state,setState]=useState(Array.isArray(citizens)?"ready":"loading"),[notice,setNotice]=useState("");
 const realtimeRef=useRef(null),loadingMore=useRef(false),presenceRef=useRef(new Map()),noticeTimer=useRef(null);
 const load=async(nextOffset=0,append=false)=>{if(loadingMore.current)return;loadingMore.current=true;try{const r=await fetch(`/api/connect-session?offset=${nextOffset}&limit=100`,{credentials:"include",cache:"no-store"});const b=await r.json().catch(()=>({}));if(!r.ok){setState(b?.error==="Authentication required"?"auth":"error");return}setList(prev=>append?[...(prev||[]),...(b.citizens||[])]:b.citizens||[]);setMessages(b.messages||[]);setOffset(nextOffset);setHasMore(Boolean(b.has_more));setState("ready")}catch{setState("error")}finally{loadingMore.current=false}};
 useEffect(()=>{if(!Array.isArray(citizens))load(0,false)},[citizens]);
 useEffect(()=>{let alive=true;(async()=>{try{const r=await fetch("/api/realtime-token",{credentials:"include",cache:"no-store"}),b=await r.json().catch(()=>({}));if(!alive||!r.ok||!b.access_token)return;await supabase.realtime.setAuth(b.access_token);const channel=supabase.channel(`merveil-live-${b.user_id}`)
 .on("postgres_changes",{event:"*",schema:"public",table:"messages"},payload=>{const m=payload.new||{},old=payload.old||{};if(payload.eventType==="DELETE"){setMessages(p=>p.filter(x=>x.lastMessageId!==old.id));return}setMessages(p=>{const idx=p.findIndex(x=>x.conversationId===m.conversation_id);if(idx<0){load(0,false);return p}const n=[...p];n[idx]={...n[idx],lastMessageId:m.id,lastMessageAt:m.created_at,lastMessagePreview:m.ciphertext?"Encrypted message":(m.body||"Message"),unread:m.sender_id!==b.user_id};return n.sort((a,c)=>new Date(c.lastMessageAt)-new Date(a.lastMessageAt))})})
 .on("postgres_changes",{event:"*",schema:"public",table:"conversations"},()=>load(0,false))
 .on("postgres_changes",{event:"*",schema:"public",table:"connections"},()=>load(0,false))
 .on("postgres_changes",{event:"*",schema:"public",table:"profiles"},payload=>{if(payload.eventType==="DELETE")return;const p=payload.new||{};setList(prev=>(prev||[]).map(x=>x.id===p.id?{...x,name:p.name||x.name,avatar:p.avatar_url||x.avatar,passportTier:normalizeTier(p.passport_tier),profession:p.profession||x.profession,country:p.country||x.country,city:p.city||x.city}:x))})
 .on("postgres_changes",{event:"*",schema:"public",table:"presence"},payload=>{const p=payload.new||{},id=p.user_id||payload.old?.user_id;if(!id)return;const next=p.status||"offline",prev=presenceRef.current.get(id);presenceRef.current.set(id,next);setList(x=>(x||[]).map(c=>c.id===id?{...c,presence:next,lastSeenAt:p.updated_at||c.lastSeenAt}:c));setMessages(x=>x.map(m=>m.participantId===id?{...m,presence:next}:m));if(prev!=="online"&&next==="online"){const person=(list||[]).find(x=>x.id===id);if(person?.connected){setNotice(`${person.name||"A citizen"} is now online`);clearTimeout(noticeTimer.current);noticeTimer.current=setTimeout(()=>setNotice(""),4500)}}})
 .subscribe();realtimeRef.current=channel}catch{}})();return()=>{alive=false;clearTimeout(noticeTimer.current);if(realtimeRef.current){supabase.removeChannel(realtimeRef.current);realtimeRef.current=null}}},[]);
 const filtered=useMemo(()=>Array.isArray(list)?list.filter(p=>`${p.name||""} ${p.role||""} ${p.location||""} ${p.profession||""} ${p.country||""}`.toLowerCase().includes(query.toLowerCase())):[],[list,query]);
 const visible=useMemo(()=>tab==="My Circle"?filtered.filter(p=>p.connected):filtered,[filtered,tab]);
 const groups=useMemo(()=>{const map={citizen:[],professional:[],investor:[],company:[]};for(const p of visible){const t=normalizeTier(p.passportTier||p.passport_tier),k=t==="services"?"professional":t;(map[k]||map.citizen).push(p)}for(const k of Object.keys(map))map[k].sort((a,b)=>(a.presence==="online"?0:a.presence==="away"?1:2)-(b.presence==="online"?0:b.presence==="away"?1:2)||new Date(b.lastSeenAt||0)-new Date(a.lastSeenAt||0));return map},[visible]);
 const guardedMessage=async p=>{const gate=localLinkGate(p?.context||"");if(gate.blocked){window.alert("Merveil blocked an unsafe link before communication.");return}try{const risk=await serverSafetyCheck({kind:"connection",known:Boolean(p?.connected),verified:Boolean(p?.verified)});if(risk.action==="warn"&&!window.confirm(`${risk.message}\n\nContinue?`))return}catch{}onMessage(p)};
 const guardedCall=async p=>{try{const risk=await serverSafetyCheck({kind:"connection",known:Boolean(p?.connected),verified:Boolean(p?.verified)});if(risk.action==="warn"&&!window.confirm(`${risk.message}\n\nContinue to call?`))return}catch{}onCall(p)};
 if(state!=="ready")return <div className="mcon"><style>{styles}</style><div className="mcon-empty">{state==="loading"?"Opening Connect…":state==="auth"?"Sign in to use your private Connect network.":"We couldn't securely load Connect."}<br/><button className="mcon-more" onClick={()=>state==="error"?load(0,false):onMessage("Sign in")}>{state==="error"?"Retry":"Continue to sign in"}</button></div></div>;
 const renderPerson=p=>{const tier=PASSPORTS[normalizeTier(p.passportTier)];return <div className="mcon-person" key={p.id}><div className="mcon-avatar">{p.avatar||initials(p.name)}</div><div className="mcon-main"><div className="mcon-name">{p.name}</div><div className="mcon-role">{p.role||p.profession||"Citizen"}{p.location?` · ${p.location}`:""}</div><div className="mcon-context">{p.context||tier.sub}</div></div><div className="mcon-right"><div className={`mcon-pres ${p.presence||"offline"}`}>● {p.presence||"offline"}</div><div className="mcon-actions"><button className="mcon-btn" onClick={()=>onOpenProfile(p)}>View</button><button className="mcon-btn primary" onClick={()=>guardedMessage(p)}>Message</button></div></div></div>};
 return <div className="mcon"><style>{styles}</style><div className="mcon-shell"><header className="mcon-head"><div><div className="mcon-kicker">Merveil</div><div className="mcon-title">Connect</div><div className="mcon-sub">Live citizens, your circle and messages — organized by Passport.</div></div><div className="mcon-kicker">{visible.filter(x=>x.presence==="online").length} online · {visible.length} total</div></header><MerveilSecurityStatus verified={e2eeVerified}/>{notice&&<div className="mcon-notice">● {notice}</div>}<div className="mcon-tabs">{["Citizens","My Circle","Messages"].map(t=><button className={`mcon-tab ${tab===t?"active":""}`} key={t} onClick={()=>setTab(t)}>{t}{t==="My Circle"?` · ${filtered.filter(x=>x.connected).length}`:""}</button>)}</div>{tab==="Messages"?<section className="mcon-card"><div className="mcon-kicker" style={{marginBottom:6}}>MESSAGES</div>{messages.length?messages.map(m=><div className={`mcon-message-row ${m.unread?"mcon-unread":""}`} key={m.conversationId} onClick={()=>onMessage({id:m.participantId,name:m.name,conversationId:m.conversationId})}><div className="mcon-avatar">{m.avatar||initials(m.name)}</div><div className="mcon-message-main"><div className="mcon-message-name">{m.name}</div><div className="mcon-message-preview">{m.lastMessagePreview}</div></div><div className="mcon-message-meta">{formatTime(m.lastMessageAt)}<br/>{m.presence||"offline"}</div></div>):<div className="mcon-empty">No conversations yet.</div>}</section>:<div className="mcon-layout"><section className="mcon-card"><input className="mcon-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search citizens, professions or countries…"/>{Object.entries(groups).map(([key,people])=>{const cfg=PASSPORTS[key];return <div className="mcon-group" key={key}><div className="mcon-group-head"><div className="mcon-group-title"><span className="mcon-dot" style={{background:cfg.color}}/>{cfg.label}<span style={{fontWeight:500,color:"#7a8490"}}>· {cfg.sub}</span></div><div className="mcon-count" style={{color:cfg.color,background:cfg.soft}}>{people.length}</div></div>{people.map(renderPerson)}</div>})}{hasMore&&<button className="mcon-more" onClick={()=>load(offset+100,true)}>Load more citizens</button>}{!visible.length&&<div className="mcon-empty">{tab==="My Circle"?"Your circle is empty.":"No citizens match this search."}</div>}</section><aside className="mcon-card mcon-intel"><div className="mcon-kicker">LIVE NETWORK</div><h3 style={{marginTop:5}}>Passport-organized Connect</h3><p>{tab==="My Circle"?"Only accepted connections are shown here. Each Passport keeps its own live section and count.":"Every Passport category stays in its own continuous section. Online and away citizens are surfaced first."}</p>{Object.entries(groups).map(([key,people])=>{const cfg=PASSPORTS[key];return <div className="mcon-suggestion" key={key}><strong style={{color:cfg.color}}>{cfg.label}</strong><span>{people.length} visible · {people.filter(p=>p.presence==="online").length} online</span></div>})}<button className="mcon-btn primary" style={{width:"100%",marginTop:12}} onClick={()=>visible[0]&&guardedCall(visible[0])}>Start a call</button></aside></div>}<div className="mcon-footer">Live presence, connections and message order use Supabase Realtime.</div></div></div>;
}